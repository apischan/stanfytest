1. Create volume:
docker volume create --name pg_data

2. Create and run own image using postgres:9.3
docker run --name postgres_test -v pg_data:/var/lib/postgresql/data/pgdata -e POSTGRES_USER=jobs -e POSTGRES_PASSWORD=mysecretpassword -e PGDATA=/var/lib/postgresql/data/pgdata -P -d postgres:9.3

3. Attach to docker console:
docker exec -u root -it postgres_test /bin/bash

docker run --rm -P --name pg_test eg_postgresql


